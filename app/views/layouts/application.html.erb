<!DOCTYPE html>
<html>

<head>
    <title>SistemaBad</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "styles" %>
    <%= javascript_include_tag "scripts", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "validarForm", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>



</head>

<body class="sb-nav-fixed">
    <% if user_signed_in? and (Usuario.find_by(user_id: current_user.id) != nil)%>
    <nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
        <!-- Navbar Brand-->
        <br>
        <br>
        <div align="center">
        <a class="navbar-brand ps-3" href="/"><img src="/assets/logo_uganda-3257afb1446cb8a797deeb7e0a7a5f0703cbe0948540646b616f5a3d7df94b0a.png" class="brand_logo" alt="Logo" width="100" height="62"></a>
        </div>
        <!-- Sidebar Toggle-->
           
        <div>
        <button class="btn btn-secondary dropdown-toggle" id="sidebarToggle" href="#!"><i
                class="fas fa-bars"></i></button>
        </div>
        <!-- Navbar Search-->
        <form class="d-none d-md-inline-block form-inline ms-auto me-0 me-md-3 my-2 my-md-0">
            <div class="input-group">
                <input class="form-control" type="text" placeholder="Search for..." aria-label="Search for..."
                    aria-describedby="btnNavbarSearch" />
                <button class="btn btn-primary" id="btnNavbarSearch" type="button"><i
                        class="fas fa-search"></i></button>
            </div>
        </form>
        <!-- Navbar-->
        <ul class="navbar-nav ms-auto ms-md-0 me-3 me-lg-4">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown"
                    aria-expanded="false"><i class="fas fa-user fa-fw"></i></a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                    <li><a class="dropdown-item" href="#!">Acción 1</a></li>
                    <li><a class="dropdown-item" href="#!">Acción 2</a></li>
                    <li>
                        <hr class="dropdown-divider" />
                    </li>
                    <li><a class="dropdown-item" href="#!">Logout</a></li>
                </ul>
            </li>
        </ul>
    </nav>
    
    <div id="layoutSidenav">
        <div id="layoutSidenav_nav">
            <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
                <div class="sb-sidenav-menu">
                    <div class="nav">
                        <div class="sb-sidenav-menu-heading">Menu</div>

                        <!--Menu de perfiles-->
                        

                        <%if can? :read, Usuario %>
                            <%= link_to  "Perfil usuarios", usuarios_path ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Rol %>
                            <%= link_to  "Roles", "/rol" ,class:'nav-link'%>
                        <%end%> 
                        <%if can? :read, Proveedor %>
                            <%= link_to  "Proveedores", proveedors_path ,class:'nav-link'%>
                        <%end%>
                        <%if can? :leer, Ubicacion %>      
                            <%= link_to  "Ubicaciones", ubicacions_path ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Reference %> 
                            <%= link_to  "Referencias", references_path ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Division %> 
                            <%= link_to  "Division admin", divisions_path ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Cargo %> 
                            <%= link_to  "Cargos", cargos_path ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Proveedor%> 
                            <%= link_to  "Reporte proveedores", reporteProveedores_path ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Producto%> 
                            <%= link_to  "Productos", "/productos" ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Producto%> 
                            <%= link_to  "Tipo de Productos", "/tipo_producto" ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Cotizacion%> 
                            <%= link_to  "Cotizaciones", "/cotizaciones" ,class:'nav-link'%>
                        <%end%>
                        <%if can? :read, Evaluacion%> 
                            <%= link_to  "Evaluaciones", "/evaluaciones" ,class:'nav-link'%><!--Falta-->
                        <%end%>


                        <!--Fin menu-->
                        
                        
                        
                        
                        
                    </div>
                </div>
                <div class="sb-sidenav-footer">
                    <div class="small">Logged in as: <%= Usuario.find_by(user_id:current_user.id).nombre.split(' ')[0] %> <%= Usuario.find_by(user_id:current_user.id).apellido.split(' ')[0] %></div>
                    <% if user_signed_in? %>
                            <%= button_to "Cerrar Sesión", destroy_user_session_path, method: :delete,  
                            class:'nav-link'%>
                    <%end%>
                </div>
            </nav>
        </div>
        <div id="layoutSidenav_content">
            <main>
                <p class="notice"><%= notice %></p>
                <p class="alert"><%= alert %></p>

                

                <%= yield %>
            </main>

        </div>

    </div>
    <% else %>

        <div style = "text-align: center;">
        <h1>Por favor, inicia sesión para ver esta pagina</h1>
        <%= button_to "Regresar al Login", destroy_user_session_path, method: :delete, class: "sb-nav-link-icon fas fa-table"%>
        </div>
    
    <% end %>




    <footer class="py-4 bg-light mt-auto">
        <div class="container-fluid px-4">
            <div class="d-flex align-items-center justify-content-between small">
                <div class="text-muted">Copyright &copy; Your Website 2022</div>
                <div>
                    <a href="#">Privacy Policy</a>
                    &middot;
                    <a href="#">Terms &amp; Conditions</a>
                </div>
            </div>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>




</html>